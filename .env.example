# Public site URL used for canonical tags, Open Graph, robots.txt and sitemap.xml.
# No trailing slash. Required in production so that SEO metadata points to the
# real domain instead of the placeholder fallback in lib/seo.js.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
